/*

// There are only 5 Falsy values in JS
// 5 Falsy Values are: 0, '', undefined, null, NaN

console.log(Boolean(0)); // false
console.log(Boolean(undefined)); //false
console.log(Boolean("aditya")); // true
console.log(Boolean({})); // true
console.log(Boolean("")); // fales

const money = 0;
if (money) {
  console.log("dont spend it all");
} else {
  console.log("you should get a job"); // will always return this bcoz 0 is falsy value
}

let height;
if (height) {
  console.log("height is defined");
} else {
  console.log("height is undefined"); // this will return bcoz height is not defined at first and undefined is also falsy value
}
*/

// const a = Number(prompt("enter a number"));
// const b = Number(prompt("enter another number"));
// console.log(`addition of ${a} and ${b} is ${a + b}`);
