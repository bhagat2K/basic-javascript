/*

const age = 18;
const drink = age >= 18 ? "wine🍷" : "water💧";
console.log(drink);

// same using if-else

let drink2;
if (age >= 18) {
  drink2 = "wine🍷";
} else {
  drink2 = "water💧";
}
console.log(drink2);

// using template literal

console.log(`I like to drink ${age >= 18 ? "wine🍷" : "water💧"}`);


// Question.

const bill = 430;
// let tip, total;

// if (bill >= 50 && bill <= 300) {
//   tip = (15 / 100) * bill;
//   total = tip + bill;
// } else {
//   tip = (20 / 100) * bill;
//   total = tip + bill;
// }

// console.log(
//   `The bill was ${bill}, the tip was ${tip} and the total value was ${total}`
// );

// tip1 = (15 / 100) * bill;
// total1 = tip1 + bill;

// tip2 = (20 / 100) * bill;
// total2 = tip2 + bill;

// console.log(
//   bill >= 50 && bill <= 300
//     ? `The bill was ${bill}, the tip was ${tip1} and the total value was ${total1}`
//     : `The bill was ${bill}, the tip was ${tip2} and the total value was ${total2}`
// );

const tip = bill <= 300 && bill >= 50 ? bill * 0.15 : bill * 0.2;
console.log(
  `The bill was ${bill}, the tip was ${tip} and the total value was ${
    bill + tip
  }`
);
*/
