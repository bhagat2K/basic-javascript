/*

"use strict";

function logger() {
  console.log("My name is Aditya");
}

logger(); // calling/running/invoking function

function fruitProcessor(apple, banana) {
  console.log(apple, banana);
  const juice = `juice with ${apple} apples and ${banana} bananas.`;
  return juice;
}

const apppleJuice = fruitProcessor(12, 87);
console.log(apppleJuice);
console.log(fruitProcessor(34, 7));

// Function declaration
function calAge(birthYear) {
  return 2024 - birthYear;
}
const age = calAge(2000);
console.log(age);

// Function expression
const calAge1 = function (birthYear) {
  return 2024 - birthYear;
};
const age1 = calAge1(1998);
console.log(age1);

// Arrow Function (function expression but shorter)
// const calAge2 = (birthYear) => 2024 - birthYear;
// const age2 = calAge2(2002);
// console.log(age2);

// const yearLeft = (birthYear) => {
//   const age = 2024 - birthYear;
//   const retire = 60 - age;
//   return retire;
// };

// console.log(yearLeft(2000));

const yearsLeft = (birthYear, name) => {
  const age = 2024 - birthYear;
  const retire = 60 - age;
  return `${name} retires in ${retire} years`;
};

console.log(yearsLeft(2000, "Aditya"));
console.log(yearsLeft(1995, "Ethan"));

function cutFruit(fruit) {
  return fruit * 4;
}
function fruitProcessor(apple, banana) {
  const applePieces = cutFruit(apple);
  const bananaPieces = cutFruit(banana);

  const juice = `Juice with ${applePieces} pieces of apple and ${bananaPieces} pieces if banana`;
  return juice;
}
console.log(fruitProcessor(4, 8));

*/

// Question.
