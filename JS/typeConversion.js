/* 

// type conversion

const year1 = "2024";
const year2 = 2025;
console.log(year1 + year2);
console.log(Number(year1) + Number(year2));

const name = "aditya";
console.log(Number(name)); // returns NaN meaning Not a Number invalid

const year3 = 2000;
const year4 = 2010;
console.log(year3 + year4); // adds both numbers
console.log(String(year3) + year4); // concatnates as one is string

// type coersion

console.log("I am " + 24 + " years old"); // "+" operator triggers it to automatically converts it to string

console.log("20" - 76 - "87"); // "-" operator triggers the opposite i.e. automatically converts to numbers (same for * and / operators)

*/
